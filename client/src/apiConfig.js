// Central configuration for API endpoints
// UNIFIED MODE: In both production and dev (with proxy), we use relative paths.
// This allows the app to work seamlessly on any domain (localhost, railway, heroku, etc.)
// without any manual configuration.

const API_BASE_URL = ''; 

export default API_BASE_URL;
